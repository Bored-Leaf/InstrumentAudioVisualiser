#include <vector>

template <typename T>
class CircularBuffer {
public:
    CircularBuffer<T> (const int size);
    bool write(const std::vector<T> &chunk);
    bool read(T& out);
private:
    std::vector<T> m_buffer{};
    int m_writeIndex{};
    int m_readIndex{};
    int m_capacity{};
    int m_size{};
};